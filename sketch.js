function setup() {
    createCanvas(400, 400);
    frameRate(10);
  }
  
  function draw() {
    background('#f0f0f0');
    strokeWeight(4);
    line(
    random(0,width), // x1
    random(0,width), // y1
    random(0,width), // x2
    random(0,width), // y2
    
    );
    //no loop
  } 